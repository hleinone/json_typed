name: json_typed
description: Typed access to decoded JSON without wrapper objects. Extension types over dart:convert maps, lists and values, with typed getters.
version: 0.0.1
repository: https://github.com/hleinone/json_typed
topics: [json]

environment:
  sdk: ^3.3.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
