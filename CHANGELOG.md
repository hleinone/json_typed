## 0.0.1

- Initial release: `JsonObject`, `JsonArray` and `JsonValue` extension types over decoded JSON, typed getters with `OrNull` variants and fallbacks, `jsonDecodeObject`, `jsonDecodeArray` and `jsonDecodeValue`, and errors that name the key and the rejected value.
